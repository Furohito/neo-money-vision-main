export const detectCurrency = async (video: HTMLVideoElement) => {
  // Dummy: selalu gagal deteksi
  return null;
};

export const initializeCurrencyDetection = async () => {
  // Dummy: selalu gagal inisialisasi
  return false;
};